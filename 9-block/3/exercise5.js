const assert = require('assert')

const myList = [1, 2, 3]
const myListObj = {a: 1, b: 2, c: 3};

const {a, b, c} = myListObj;
console.log(`${a} ${b} ${c}`);

// escreva swap abaixo
// const swap = (arr) => {};

// const swappedList = swap(myList);

// assert.equal(swappedList[0], 3)
// assert.equal(swappedList[1], 2)
// assert.equal(swappedList[2], 1)
    
